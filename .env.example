# Public origin of the deployed site. Used for canonical URLs, the sitemap and Open Graph tags.
SITE_URL=https://example.com

# Only set this when the site is served from a sub-path (GitHub Pages project sites).
# BASE_PATH=/portfolio

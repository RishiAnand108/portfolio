---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<span
  class:list={[
    'border-line text-muted inline-flex items-center rounded-full border px-2.5 py-0.5 font-mono text-xs whitespace-nowrap',
    className,
  ]}
>
  <slot />
</span>
